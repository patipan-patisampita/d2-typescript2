// let add: (x: number, y: number) => number =
//     function (x: number, y: number) {
//         return x + y;
//     }
var add = function (x, y) {
    return x + y;
};
var total = add(2, 5);
console.log(total);
