// let add: (x: number, y: number) => number =
//     function (x: number, y: number) {
//         return x + y;
//     }
let a = 5;
let b: number = 5;
let add = function(x: number, y:number):number {
    return x + y;
}
let total: number = add(2, 5)

console.log(total);

